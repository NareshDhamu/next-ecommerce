/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com", "images.pexels.com" , "static.wixstatic.com", "app-uploads-cdn.fera.ai", "people.pic1.co"],
    }
};

export default nextConfig;
